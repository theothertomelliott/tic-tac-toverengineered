FROM alpine
WORKDIR /root

docker:
    ARG VERSION=dev
    ARG REGISTRY
    ARG IMAGE_REF=$REGISTRY/grid-image:$VERSION
    BUILD --build-arg BINARY=grid --build-arg VERSION=$VERSION --build-arg IMAGE_REF=$IMAGE_REF ../../+docker