FROM alpine
WORKDIR /root

docker:
    ARG VERSION=dev
    ARG IMAGE_REF=space-image:$VERSION
    BUILD --build-arg BINARY=space --build-arg VERSION=$VERSION --build-arg IMAGE_REF=$IMAGE_REF ../../+docker