config:
  target: http://localhost:8080
  phases:
    - name: "Run Games"
      arrivalRate: 4
      duration: 10
  engines:
    playwright: { aggregateByName: true } 
  processor: "../playwright/game.js"
scenarios:
  - name: game
    engine: playwright
    flowFunction: "playGame"
    flow: []